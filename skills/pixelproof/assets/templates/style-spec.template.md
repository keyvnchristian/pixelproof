# {{PROJECT}} — Design System (Exact Spec)

> Generated by the pixelproof skill from measured, rendered slices. Every value in this document comes from the slices in `{{DIR}}/slices/`.

## Contents
0. Rules
1. Wiring
2. CSS
3. Icons
4. Components
5. Which pattern to use
6. Measurement cheat sheet
7. Responsive
8. Verification

## References

{{REFERENCES}}

<!-- WRITE: One paragraph:
- what each reference demonstrates
- brand overrides (e.g. "primary is the project crimson #992c29 instead of the reference blue")
- for restyles: "the reference pages contain demo content; copy the style, never the content" -->

## 0. Rules
1. **Read first.** Read the slices before writing UI code. The slices are the source of truth; if this document and a slice disagree, the slice wins.
2. **Values.** Use the CSS in Section 2 through the project's theme tokens. Computed values must stay identical, so don't round or approximate anything.
3. **Icons.** Use only icons from the sprite in Section 3. No icon libraries. If a new icon is needed, draw it in the same style and add it to the sprite.
4. **Markup.** Reuse the class names and markup in Section 4. Build new screens by composing these components.
5. **New elements.** Build anything not covered here from existing tokens and patterns. No new colors, shadows, gradients, radii, or font sizes.
6. **Colors.** <!-- WRITE: where each accent/status/special color is allowed -->
7. **Shadows and gradients.** <!-- WRITE: the only allowed shadows/gradients and where -->
8. **Verify.** Run Section 8 before reporting any UI work as done.

## 1. Wiring
- **Tokens:** the CSS in Section 2 is ported into the project's three-layer theme (primitives → semantic → component). Components use only semantic and component tokens.
- **Font:** <!-- WRITE: family, weights, loading method, fallback stack -->
- **Icons:** render the sprite once at the app root, and use an `Icon` component that outputs `<svg class="icon"><use href="#i-name"/></svg>`.
- **Tailwind (if used):** layout only. Component visuals come from these classes.
- **State:** state lives in attributes and classes: `aria-selected`, `aria-pressed`, `aria-expanded`, `aria-current="page"`, `.is-active`, `.is-disabled`.

## 2. The CSS (source of truth)

```css
{{CSS}}
```

## 3. Icons

Icons: {{ICON_LIST}}

```html
{{SPRITE}}
```

{{ICON_TABLE}}

**Where each icon is used**

<!-- WRITE: table | Place | Icon | Size | Color | covering every icon use in the slices -->

## 4. Components

{{COMPONENTS}}

<!-- WRITE: after each component above (or here, grouped), add notes:
- variants and modifiers
- content rules (e.g. status is two-line text, not pills)
- thumbnails/avatars fallback
- column grids and how headers line up with rows
- anything a builder could get wrong -->

## 5. Which pattern to use

<!-- WRITE:
| Need | Use |
|---|---|
| … | … |
Then the page layout order (top to bottom), and the detail-page recipe if the system has no detail component. -->

## 6. Measurement cheat sheet

### Borders
<!-- WRITE: | Element | Border | — every bordered element, which sides, color; and which elements have NO border -->

### Heights, padding, gaps, radii, type
<!-- WRITE: | Element | Height | Padding / spacing | Radius | Font | — from measurements.md and the CSS -->

## 7. Responsive
<!-- WRITE: per breakpoint, what changes. Mark rules that were derived (not in the references) as "derived".
State that only specific wrappers may scroll sideways; the page never does. -->

## 8. Verification

1. **Visual check:** render the app page next to the matching slice at 1440, 1024 and 390px and compare the screenshots.
2. **Computed styles:** these must match exactly (the widths shown are informational).

{{VERIFY}}

3. **Column alignment:** header labels line up with row content (note any documented offsets).
4. **Icons:** only sprite icons, with no icon-library imports.
5. **Overflow:** no horizontal page overflow at 1024 and 390px.
6. **Theme:** no raw colors or px values outside the theme files.
7. **Report:** every difference found and how it was fixed.
